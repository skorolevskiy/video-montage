version: "3.8"

services:
  video-montage:
    build: .
    container_name: video-montage
    ports:
      - "8000:8000"
    environment:
      - PERSISTENT_DIR=/app/storage
    volumes:
      - ./storage:/app/storage